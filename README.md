# Final Ingeniería de Software

App mínima HTML que muestra Nombre, Apellido y DNI.

## Ejecutar con Docker

### Construir imagen
```bash
docker build -t final-ingsoft .

## Acceso a la aplicación

Con el contenedor en ejecución, la aplicación se puede visualizar desde un navegador web ingresando a:

http://localhost:8080

Para verificar que el contenedor se encuentra activo:

```bash
docker ps